name: ai-review
on:
  pull_request_target:
    types: [opened, ready_for_review, synchronize]
permissions:
  contents: write
  pull-requests: write
  issues: write
jobs:
  ai-review:
    uses: vladm3105/aidoc-flow-operations/.github/workflows/ai-review.yml@main
    secrets: inherit  # pragma: allowlist secret
