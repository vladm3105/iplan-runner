[pytest]
# importlib mode lets the two self-contained engines keep identically named
# test files (test_validation.py, test_integration.py) without collection clashes.
addopts = --import-mode=importlib
